name: hello github actions

on:
  push:
  schedule:
    - cron: */1 * * * *

jobs:
  bot:
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout codes'
        uses: actions/checkout@v1
      - name: 'Print String'
        run: echo "hello github actions"
